// 8. Write a program to find next Prime number of a given number
#include<stdio.h>
int main()
{
    int i,j,isprime,num;
    printf("Enter the number: ");
    scanf("%d",&num);
    for ( i = 2; i < num; i++)
    {
        isprime = 1;
        for ( j = 2; j < i; j++)
        {
            
        }
        
    }
    
}